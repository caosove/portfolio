package it.will.kingsmap.dao.mapper;

import java.util.List;

import it.will.kingsmap_mybatis.dto.AdminEmail;
import it.will.kingsmap_mybatis.dto.AptDTO;
import it.will.kingsmap_mybatis.dto.DandockDTO;
import it.will.kingsmap_mybatis.dto.GongsiDTO;
import it.will.kingsmap_mybatis.dto.VillaDTO;

public interface AdminMapper {
	
	int addApart(List<AptDTO> list);
	int addVilla(List<VillaDTO> list);
	int addDandock(List<DandockDTO> list);
	int addGongsi(List<GongsiDTO> list);
	
	
	AdminEmail getAdmin(String email);
	int setCode(AdminEmail admin);
}
